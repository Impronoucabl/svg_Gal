use std::f64::consts::PI;
use std::rc::Rc;

use crate::gall_errors::{Error, GallError};

pub struct GallWord {
    // loc: GallLoc,
    // tainer_vec: Vec<GallTainer>,
    // radius: Rc<PositiveDist>,
    // thickness: Rc<PositiveDist>,
}

impl GallWord {
    pub fn new(word:String) -> GallWord {
        GallWord{}
    } 
}